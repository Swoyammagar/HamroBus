import React from 'react';
import { View, Text } from 'react-native';

const Drivers = () => (
  <View style={{ padding: 20 }}>
    <Text style={{ fontSize: 18, fontWeight: '600' }}>Drivers</Text>
    <Text style={{ marginTop: 8 }}>No drivers yet — content placeholder.</Text>
  </View>
);

export default Drivers;
