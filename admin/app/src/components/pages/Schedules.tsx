import React from 'react';
import { View, Text } from 'react-native';

const Schedules = () => (
  <View style={{ padding: 20 }}>
    <Text style={{ fontSize: 18, fontWeight: '600' }}>Schedules</Text>
    <Text style={{ marginTop: 8 }}>No schedules yet — content placeholder.</Text>
  </View>
);

export default Schedules;
